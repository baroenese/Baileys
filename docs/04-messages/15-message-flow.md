# 15. Message Flow

> **Tujuan**: Memahami perjalanan lengkap sebuah pesan dari A ke Z.

## 📨 Sending Message Flow

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Your Code                                                 │
│    await sock.sendMessage(jid, { text: 'Hello' })           │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Message Generation (messages-send.ts)                    │
│    - Validate content                                        │
│    - Generate message ID                                     │
│    - Add timestamp                                           │
│    - Create proto.Message object                            │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Recipient Device Discovery                               │
│    - Fetch user's devices (from cache/server)               │
│    - Example: User has phone + web                          │
│      → [1234567890:0, 1234567890:1]                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Encryption (Per Device)                                  │
│    For each device:                                          │
│    - Load Signal session                                     │
│    - Encrypt message with Double Ratchet                    │
│    - Create ciphertext                                       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. Binary Node Construction                                 │
│    {                                                         │
│      tag: 'message',                                         │
│      attrs: { to: jid, type: 'text', id: msgId },          │
│      content: [encrypted_payload_for_each_device]           │
│    }                                                         │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. Binary Encoding                                           │
│    - Convert to binary format (WABinary)                    │
│    - Token compression                                       │
│    - Size: ~100 bytes (vs ~500 bytes JSON)                 │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. Noise Encryption                                          │
│    - Encrypt entire binary frame                            │
│    - Add MAC for integrity                                   │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. WebSocket Send                                            │
│    ws.send(encrypted_frame)                                  │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
                    WhatsApp Server
```

## 📥 Receiving Message Flow

```
                    WhatsApp Server
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 1. WebSocket Receive                                         │
│    ws.on('message', data)                                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Noise Decryption                                          │
│    - Verify MAC                                              │
│    - Decrypt frame                                           │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Binary Decoding                                           │
│    - Decompress if needed (zlib)                            │
│    - Parse binary to BinaryNode                             │
│    - Decode tokens to strings                               │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Route by Tag                                              │
│    if (tag === 'message') → process message                 │
│    if (tag === 'receipt') → process receipt                 │
│    if (tag === 'notification') → process notification       │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. Signal Decryption                                         │
│    - Load session for sender                                 │
│    - Decrypt ciphertext                                      │
│    - Update ratchet state                                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. Protobuf Decode                                           │
│    proto.Message.decode(decrypted_payload)                  │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. Event Emission                                            │
│    ev.emit('messages.upsert', {                             │
│      messages: [message],                                    │
│      type: 'notify'                                          │
│    })                                                        │
└───────────────────────────┬─────────────────────────────────┘
                            │
                            ▼
                    Your Event Handler
```

## 🔄 Retry Flow (Decryption Failed)

```
Message Received → Decryption Failed
        ↓
Send Retry Request to Sender
        ↓
Sender's sock.getMessage(key) called
        ↓
Re-encrypt with fresh keys
        ↓
Re-send message
        ↓
Recipient receives → Try decrypt again
```

**Max retries**: 5 times (configurable)

## ⏱️ Timing Benchmarks

```
Total Send Time: 50-200ms
├─ Device lookup: 5-50ms (with cache: <1ms)
├─ Encryption: 10-30ms per device
├─ Binary encoding: <1ms
├─ Network: 30-100ms
└─ Receipt: +50ms
```

## 💡 Optimization Tips

### 1. Cache Device Lists
```typescript
const userDevicesCache = new NodeCache({ stdTTL: 300 })
// 5-50ms → <1ms (50x faster!)
```

### 2. Batch Messages
```typescript
// Instead of 10 sequential sends (500ms each)
// Send in parallel (500ms total)
await Promise.all(jids.map(jid => 
  sock.sendMessage(jid, content)
))
```

### 3. Use Connection Pool
```typescript
// Multiple socket instances for high throughput
const sockPool = [sock1, sock2, sock3]
const sock = sockPool[msgCount % sockPool.length]
```

## 🔄 Next Steps

- [16. Encryption Process](./16-encryption.md) - Deep dive encryption
- [17. Retry System](./17-retry-system.md) - Handle failures
- [18. getMessage Implementation](./18-getmessage.md) - Message store
